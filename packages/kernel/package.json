{
  "name": "@loom/kernel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "lint:no-ambient-clock": "node scripts/no-ambient-clock.mjs",
    "test": "tsc && pnpm lint:no-ambient-clock && node --experimental-sqlite --no-warnings --test dist/test/*.js"
  }
}
