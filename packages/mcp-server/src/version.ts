// Three version constants surfaced by `pipeline_meta`. They sit
// together in their own module so a future swap to a runtime
// `KERNEL_SCHEMA_VERSION` export touches one file rather than the
// meta handler body.

export const PROTOCOL_VERSION = "3.0.0";
export const PLUGIN_API_VERSION = "3.0.0";
export const KERNEL_VERSION = "3.0.0";
